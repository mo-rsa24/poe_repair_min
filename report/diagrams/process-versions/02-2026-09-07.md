# Process lane 02: 2026-09-07

Changed since version 01: the whole map moved from the Glossy art direction to Empty, so every
prompt lost its style paragraph and describes its subject in its own words instead. The lane also
gained "How the corrector's number was made" as prompt 9, written by a parallel report-pulse run.

## Process lane

Navigation: ⬅️ [Previous](#subject-capstone-the-findings-folder-on-one-page) | 📋 [TOC](#table-of-contents) | [Next](#prompt-1-process-rung-1-when-the-product-works) ➡️

The eight rungs of the reading arc, one picture each, plus the corrector measurement, closed by
the whole arc as one line. History: `diagrams/process-versions/`. This lane is regenerated, never
patched: it is keyed to the arc in [the report index](00-INDEX.md#read-it-in-this-order), so any
change to the arc rewrites the lane.

A rung is marked `[built]` when a written finding file backs every claim in its picture, and
`[planned]` when the rung's evidence is on disk but no finding file has been written yet. Rungs 1,
2, 3 and 8 are the `[planned]` ones, which is the same hole the arc marks with ⬜.

### Prompt 1 (Process): Rung 1, when the product works

[planned] ⏳ not rendered
Save as: `diagrams/report-p01-when-the-product-works.png`

```
Style: none prescribed. Do not describe a visual style, color palette, icon set, or rendering technique. Let the composition and visual treatment that best communicate this subject emerge from the context below, rather than from an imposed convention.

Scene: landscape, 16:9. This is rung 1 of an eight-rung argument, and its whole job is to show the successful case before any failure appears. Two ways of asking a text-to-image model for two things at once are shown on the left. The first multiplies together what two separate prompts predict: "a butterfly" and "a meadow". The second uses one sentence naming both things at once: "a cat and a dog". Both feed a detector that counts how many animals are in a picture. On the right, the detector's output: sixteen generated pictures in two rows of eight, one row per method, one column per random starting seed. Every picture in the top row shows a butterfly above a meadow; every picture in the bottom row shows a cat and a dog as two separate animals. All sixteen are marked as having passed.

Cast: two separate text prompts multiplied together (plain product-of-experts); one joined prompt naming both animals in a single sentence; the detector that counts animals; sixteen generated pictures, all successful.

Flows: the product-of-experts path from two separate prompts through a multiply step to the detector. The joined-prompt path from one sentence directly to the detector. Tell the two paths apart consistently; they are the two conditions everything later is measured against.

Text in the image: title "rung 1: this is what working looks like". Subtitle: "the two columns everything else is measured against". Labels: "a butterfly", "a meadow", "multiply", "plain product of experts", "a cat and a dog", "joined prompt", "counts two", "8 of 8 seeds", "8 of 8 seeds".

Exclusions: no failed or fused animal anywhere. No picture marked as failing. No adapter or correction of any kind: neither exists yet at this rung. No charts or curves. No placeholder or lorem text. No watermark.
```

Faithfulness note: every one of the sixteen pictures must be marked as passing and no fused animal may appear, because this rung's entire job is the successful case before any failure is introduced. The multiplied path must carry the butterfly pair and the joined-prompt path the cat and dog, because plain product-of-experts composes the control pair and only the joined prompt composes cat and dog.

### Prompt 2 (Process): Rung 2, where it breaks and how we know

Navigation: ⬅️ [Previous](#prompt-1-process-rung-1-when-the-product-works) | 📋 [TOC](#table-of-contents) | [Next](#prompt-3-process-rung-3-the-four-checks-that-make-it-causal) ➡️

[planned] ⏳ not rendered
Save as: `diagrams/report-p02-where-it-breaks.png`

```
Style: none prescribed. Do not describe a visual style, color palette, icon set, or rendering technique. Let the composition and visual treatment that best communicate this subject emerge from the context below, rather than from an imposed convention.

Scene: landscape, 16:9. Rung 2 of an eight-rung argument. On the left, two separate prompts, "a cat" and "a dog", have their predictions multiplied together. That single path feeds a large grid filling the centre of the frame: seventeen rows by eight columns, one row per animal pair and one column per random starting seed, 136 pictures in total. Every picture in the grid shows one single animal whose left side carries one species' features and whose right side carries the other's, a fused creature rather than two animals. Every picture is marked as failing. On the right, the detector that produced those verdicts, showing a count of one animal. Below the detector, and feeding into it rather than into the grid, the set of hand-labelled images the detector was checked against before it was trusted.

Cast: two separate prompts multiplied together; a gallery of 136 uncorrected renders covering 17 animal pairs at 8 seeds each; the detector that counts animals; the hand-labelled validation set that the detector was checked against.

Flows: the multiplied path into the grid, meaning every picture in the grid was made this one way. The labelled set into the detector, meaning the instrument was validated before it was trusted. These two are different kinds of connection and must not be confused.

Text in the image: title "rung 2: one animal where two were asked for". Labels: "a cat", "a dog", "multiply", "17 pairs x 8 seeds = 136 renders", "counts one", "the instrument, checked against labelled images first".

Exclusions: no correction and no adapter: neither exists yet at this rung. No picture in the grid showing two separate animals. No connection from the labelled set into the grid. No charts or curves. No placeholder or lorem text. No watermark.
```

Faithfulness note: every picture in the grid must show a single fused body rather than two animals sharing a frame, because a blend and a miss are different failures and only the blend is what this project is about. The validation set must feed the detector and not the grid, or the picture claims the labelled images produced the renders.

### Prompt 3 (Process): Rung 3, the four checks that make it causal

Navigation: ⬅️ [Previous](#prompt-2-process-rung-2-where-it-breaks-and-how-we-know) | 📋 [TOC](#table-of-contents) | [Next](#prompt-4-process-rung-4-what-the-correction-is-made-of) ➡️

[planned] ⏳ not rendered
Save as: `diagrams/report-p03-four-checks.png`

```
Style: none prescribed. Do not describe a visual style, color palette, icon set, or rendering technique. Let the composition and visual treatment that best communicate this subject emerge from the context below, rather than from an imposed convention.

Scene: landscape, 16:9. Rung 3 of an eight-rung argument. At the top, one quantity is defined by subtraction: what the joined prompt predicts minus what plain product-of-experts predicts, at every one of the 50 denoising steps. That quantity is called the correction. It feeds four separate checks arranged left to right below it, all four acting on the same object. Check one adds increasing amounts of it and shows four resulting pictures, the leftmost failing and each one closer to two separate animals until the rightmost passes. Check two compares the pair's own real correction against four substitutes of exactly the same size pointing in other directions: the real one produces a passing picture, all four substitutes produce failing ones. Check three shows the 50 denoising steps as a left-to-right progression from noise to finished picture, with the correction allowed to act only over the first fifth, marked as passing, and separately only over the last fifth, marked as failing. Check four shows three independent measurements agreeing on the same answer.

Cast: the correction, defined as the joined-prompt prediction minus the plain product-of-experts prediction; a dial controlling how much of it is added back; four substitutes matched in size but pointing elsewhere; the 50 denoising steps with an early window and a late window; three independent reads that agree.

Flows: the correction feeding all four checks, meaning all four are asking about one object, not four different ones.

Text in the image: title "rung 3: four ways of asking whether the correction is the cause". Check titles: "more of it", "the same size, pointing elsewhere", "only early", "three reads agree". Labels: "the joint prompt minus plain product of experts", "how much is added back", "own correction", "other pair", "other seed", "steps shuffled", "random vector", "noise", "finished image", "steps 0 to 10", "steps 40 to 50".

Exclusions: no adapter anywhere in this image; the correction here is taken from a stored joined-prompt run, not predicted by anything trained. No substitute drawn smaller than the real correction. No plotted data points inside the third check. No placeholder or lorem text. No watermark.
```

Faithfulness note: the four substitutes must be drawn at the same size as the real correction and differ only in direction, because they are matched in size; drawing them smaller would teach that the control failed for lack of magnitude. The early window must pass and the late window fail, because the correction works early and not late. No adapter appears at this rung.

### Prompt 4 (Process): Rung 4, what the correction is made of

Navigation: ⬅️ [Previous](#prompt-3-process-rung-3-the-four-checks-that-make-it-causal) | 📋 [TOC](#table-of-contents) | [Next](#prompt-5-process-rung-5-the-adapter-and-the-checkpoint-to-show) ➡️

[built] ⏳ not rendered
Save as: `diagrams/report-p04-what-its-made-of.png`

```
Style: none prescribed. Do not describe a visual style, color palette, icon set, or rendering technique. Let the composition and visual treatment that best communicate this subject emerge from the context below, rather than from an imposed convention.

Scene: landscape, 16:9, two halves. The left half answers what the correction is made of. Product-of-experts already computes three predictions, and everything they can build between them forms a flat plane. The correction is drawn as an arrow leaving that plane at a modest angle, mostly lying within it but not entirely. Beside it, a proportion showing that 0.374 of the correction's squared length lies outside the plane, so roughly a third is new and roughly two thirds is not. The right half answers where the correction moves the picture to. It shows the detector's own image space as an open field holding three separate clusters of points: plain product-of-experts at the lower left, the corrected runs in the middle, and the joined prompt at the upper right. An arrow runs from the uncorrected cluster to the corrected one. A second arrow continues from the corrected cluster toward the joined-prompt cluster but stops short and does not arrive. Two example pictures sit at the field's edge, each joined to its own cluster: one fused animal for the uncorrected cluster, two separate animals for the corrected one.

Cast: the three predictions product-of-experts already has, and the flat plane of everything they can build; the correction, leaving that plane at an angle; the share lying outside it, 0.374; the detector's image space; three clusters, uncorrected, corrected, and joined prompt; two example pictures.

Flows: a completed move from the uncorrected cluster to the corrected one, meaning the correction does move the render. An incomplete move from the corrected cluster toward the joined-prompt cluster, meaning the remaining distance was not closed.

Text in the image: title "rung 4: what the correction is made of, and where it lands". Half titles: "outside what the product already has", "in the scorer's image space". Labels: "the three predictions the product has", "the correction", "0.374 outside the span", "plain product of experts", "corrected", "joint prompt", "one fused animal", "two separate animals", "distance not closed".

Exclusions: no adapter, no denoising steps, no dial: this rung is about the correction itself, measured from stored runs. The second arrow must not reach the joined-prompt cluster. No axis ticks or gridlines on the field. No numbers other than 0.374. No placeholder or lorem text. No watermark.
```

Faithfulness note: the correction must leave the plane at a modest angle rather than perpendicular to it, and the proportion must read as roughly a third, because 0.374 of the correction's squared length lies outside the plane and most of it does not. The arrow from corrected toward joined prompt must stop short and not arrive, because the correction moves the render toward the joined prompt without reaching it.

### Prompt 5 (Process): Rung 5, the adapter and the checkpoint to show

Navigation: ⬅️ [Previous](#prompt-4-process-rung-4-what-the-correction-is-made-of) | 📋 [TOC](#table-of-contents) | [Next](#prompt-6-process-rung-6-the-pairs-it-never-trained-on) ➡️

[built] ⏳ not rendered
Save as: `diagrams/report-p05-which-checkpoint.png`

```
Style: none prescribed. Do not describe a visual style, color palette, icon set, or rendering technique. Let the composition and visual treatment that best communicate this subject emerge from the context below, rather than from an imposed convention.

Scene: landscape, 16:9. Rung 5 of an eight-rung argument. A single long axis runs the full width of the frame, left to right, standing for how long a small adapter has been trained. The same adapter appears at three points along it: early, middle, and far right. Above the early one, the picture it produces: a single fused animal, marked as failing. Above the middle one, the picture it produces: a cat and a dog as two separate animals, sharp and clean, marked as passing, and this point is flagged as the one chosen for the paper. Above the far-right one, the picture it produces: still two separate animals, but visibly hazy and washed out, marked as failing. Running beneath the whole axis, a quantity that grows steadily thicker from left to right along its entire length including past the flagged point, standing for how large the adapter's weights have become. At the far left, outside the axis, the stored training targets the adapter learns from, connected to the earliest point.

Cast: the training axis; one adapter shown at three training lengths; the three pictures they produce; the flag marking the checkpoint chosen for the paper; the adapter's weight size, growing without bound; the stored correction targets it trains on.

Flows: the stored targets feeding the adapter at the start, which are read from a cache rather than computed live. Nothing else flows; the three points are one adapter at three ages, not three components passing something along.

Text in the image: title "rung 5: further is not better". Labels: "training steps", "early: not yet composing", "the checkpoint we show: 7 of 8 seeds", "late: composes but drifts", "weight norm, growing without bound", "stored targets".

Exclusions: no plotted curve with axes. No arrows between the three adapter positions, which would make one adapter look like three. The growing quantity must not stop or level off at the flagged point. The far-right picture must show two animals, not one. No placeholder or lorem text. No watermark.
```

Faithfulness note: the weight size must keep growing across the entire axis including past the flagged checkpoint, because the weights keep growing after the useful point and the gap between "still growing" and "no longer improving" is the finding. The rightmost picture must show two separate animals and still be marked as failing, since the late failure is loss of fidelity, not loss of composition.

### Prompt 6 (Process): Rung 6, the pairs it never trained on

Navigation: ⬅️ [Previous](#prompt-5-process-rung-5-the-adapter-and-the-checkpoint-to-show) | 📋 [TOC](#table-of-contents) | [Next](#prompt-7-process-rung-7-the-ten-cheaper-fixes-that-did-not-work) ➡️

[built] ⏳ not rendered
Save as: `diagrams/report-p06-unseen-pairs.png`

```
Style: none prescribed. Do not describe a visual style, color palette, icon set, or rendering technique. Let the composition and visual treatment that best communicate this subject emerge from the context below, rather than from an imposed convention.

Scene: landscape, 16:9. Rung 6 of an eight-rung argument. On the left, the pool of animal pairs one adapter was trained on. That single adapter then meets three kinds of pair it was never trained on, drawn as three separate destinations to its right. The first destination is pairs built from words it saw, in a pairing it saw: its pictures all pass, and it fits those at 0.97. The second is pairs built from words it saw, in a pairing it did not: its pictures are mixed pass and fail, and it fits at 0.84. The third is pairs with no word it ever saw: its pictures all fail, and it fits at 0.80. Separately, at the far right and connected to nothing, a question that was asked and could not be answered: a measurement read from the cache that was supposed to predict which pairs would fail, drawn as reading the same at both of its extremes across pairs that all failed identically.

Cast: the pool of training pairs; one pooled adapter; three kinds of unseen pair with their fit numbers 0.97, 0.84 and 0.80; the interaction-strength measurement that failed to separate the pairs.

Flows: one path from the training pool into the adapter, then splitting three ways, meaning one adapter meets three kinds of unseen pair.

Text in the image: title "rung 6: it carries to some pairs and not others". Labels: "the pairs it trained on", "one pooled adapter", "seen words, seen pairing", "seen words, new pairing", "no seen word", "0.97", "0.84", "0.80", "how well it fits", "does interaction strength predict which pairs blend? no".

Exclusions: the failed measurement must not be drawn as separating the pairs. No plotted curve with axes. No numbers other than 0.97, 0.84 and 0.80. No placeholder or lorem text. No watermark.
```

Faithfulness note: the three fit numbers must descend left to right as 0.97, 0.84, 0.80 against their three destinations, and the interaction-strength measurement must visibly read the same at both ends, because it spans a wide range across pairs that all fail identically. Drawing it as separating the pairs would invert the null.

### Prompt 7 (Process): Rung 7, the ten cheaper fixes that did not work

Navigation: ⬅️ [Previous](#prompt-6-process-rung-6-the-pairs-it-never-trained-on) | 📋 [TOC](#table-of-contents) | [Next](#prompt-8-process-rung-8-what-is-still-open) ➡️

[built] ⏳ not rendered
Save as: `diagrams/report-p07-ten-cheaper-fixes.png`

```
Style: none prescribed. Do not describe a visual style, color palette, icon set, or rendering technique. Let the composition and visual treatment that best communicate this subject emerge from the context below, rather than from an imposed convention.

Scene: landscape, 16:9. Rung 7 of an eight-rung argument. A single path enters at the left carrying a picture of one fused animal. It passes through three gateways in turn, and leaves at the right edge still carrying a picture of one fused animal, entirely unchanged. Gateway one holds three attempts that re-sample the image without changing what the model predicts. Gateway two holds three attempts using a different method of combining the two prompts. Gateway three holds four attempts that spend extra computation at generation time choosing among candidates. Each of the ten attempts carries its outcome: nine came back null or inconclusive, and exactly one, in the third gateway, passed. Running above the main path and never touching it, a separate short path carries a picture of two separate animals: the learned correction, which is the thing that does work.

Cast: the uncorrected path carrying a fused animal; three gateways, which are correctors that re-sample without changing the score, a different composition method called SuperDiff, and search and selection at generation time; ten attempts with their outcomes, nine failing and one passing; the learned correction on its own separate path.

Flows: one path running the full width, arriving and departing unchanged. One separate short path above it that never joins the first.

Text in the image: title "rung 7: ten ways of avoiding the correction". Gateway titles: "correctors: re-sample without changing the score", "another method: SuperDiff", "search and selection: spend compute at inference". Labels: "one fused animal", "still one fused animal", "the learned correction", "two separate animals", "9 of 10 null or inconclusive", "1 of 10 sharpens what already composed".

Exclusions: exactly one of the ten attempts may be marked as passing, and it must be in the third gateway. The main path must not change what it carries. No adapter inside any gateway. No plotted curve with axes. No placeholder or lorem text. No watermark.
```

Faithfulness note: the main path must arrive and depart carrying the same fused animal, because none of the ten alternatives turned one animal into two. The single passing attempt must sit in the third gateway, because the one support verdict is noise search applied on top of an adapter that was already composing, which is a sharpening result and not a composing one. Exactly one pass, or the picture overstates the rung.

### Prompt 8 (Process): Rung 8, what is still open

Navigation: ⬅️ [Previous](#prompt-7-process-rung-7-the-ten-cheaper-fixes-that-did-not-work) | 📋 [TOC](#table-of-contents) | [Next](#prompt-9-process-how-the-correctors-number-was-made) ➡️

[planned] ⏳ not rendered
Save as: `diagrams/report-p08-still-open.png`

```
Style: none prescribed. Do not describe a visual style, color palette, icon set, or rendering technique. Let the composition and visual treatment that best communicate this subject emerge from the context below, rather than from an imposed convention.

Scene: landscape, 16:9. Rung 8 of an eight-rung argument, and it is an inventory of unfinished work rather than a route. Across the upper part of the frame, four results that have their figures and their numbers already but no write-up: each is drawn as complete in every respect except that the place where its verdict belongs is visibly empty. Across the lower part, three separate loose ends: a recipe that does not exist, so one number cannot be regenerated from scratch; a comparison showing that the adapter spends about half the energy the true correction spends; and three training runs stopped before their planned end, one of them at 61,000 steps out of a planned 100,000. A person stands at the right edge with an open notebook.

Cast: four results awaiting write-up, which are how much correction is needed, the same story from three sides, whether one pooled fix transfers at all, and charging the adapter for its energy; one missing recipe; the energy comparison between adapter and true correction; three trainings stopped early; the researcher.

Flows: none. This is a list of outstanding work, not a sequence.

Text in the image: title "rung 8: verdicts on disk with nowhere to read them". Labels: "how much correction is needed", "the same story from three sides", "does one pooled fix transfer at all", "charging the adapter for its energy", "no recipe regenerates this number", "the adapter spends half what the correction spends", "stopped at 61k of 100k".

Exclusions: no verdict of any kind attached to any of the four results; the empty verdict place is the point. No arrows. No result drawn as blank or empty overall, since the experiments were run and only the write-up is missing. No placeholder or lorem text. No watermark.
```

Faithfulness note: each of the four results must be visibly complete apart from an empty verdict place, because the numbers and figures exist and only the finding file does not; drawing them as blank would claim the experiments were never run. No verdict may appear anywhere, since assigning one is exactly the work outstanding.

### Prompt 9 (Process): How the corrector's number was made

Navigation: ⬅️ [Previous](#prompt-8-process-rung-8-what-is-still-open) | 📋 [TOC](#table-of-contents) | [Next](#process-capstone-the-whole-arc-as-one-line) ➡️

[built] ⏳ not rendered
Save as: `diagrams/report-p09-how-the-correctors-number-was-made.png`

```
Style: none prescribed. Do not describe a visual style, color palette, icon set, or rendering technique. Let the composition and visual treatment that best communicate this subject emerge from the context below, rather than from an imposed convention.

Scene: landscape, 16:9, read left to right in four stages of increasing width. Stage one is a single starting noise, pinned and shared. Stage two shows six entirely separate runs, all beginning from that same pinned noise: each run is the 50 denoising steps with a different number of corrector repetitions inserted at every step, and the six counts are 0, 1, 5, 20, 100 and 200. It must be unmistakable that these are six independent runs rather than six moments of one run. Stage three shows, for each run, the size of the correction measured at every one of the 50 steps: the last five steps are marked as the zone where the reading is licensed, and the first ten are marked as the zone where the model's error and the sampler's error cannot be told apart. Stage four is a three-way verdict with slots for support, null and inconclusive, where only the inconclusive slot is filled. Spanning stages two and three from below, a note that all six runs sat on one graphics card, with a warning attached.

Cast: the pinned starting noise shared by every run; six separate runs at corrector counts 0, 1, 5, 20, 100 and 200; the per-step size of the correction each run produces; the licensed read zone of the last five steps; the un-attributable window of the first ten; the three-way verdict with two unused slots; the single-device requirement.

Flows: the pinned noise into every one of the six runs, meaning all six start identically. Each run contributing one curve into the measurement stage. The licensed read zone into the verdict.

Text in the image: title "one seed, six runs, one number that could not be read". Stage titles: "the pinned noise", "six runs, not six moments", "the size at every step", "the three-way bar". Labels: "corrector count", "0", "1", "5", "20", "100", "200", "the last five steps: the model's share", "the first ten: the two errors cannot be told apart", "support", "null", "inconclusive", "one device for all six, or the numbers do not compare".

Exclusions: no connection between any two of the six runs. No verdict in the support or null slot. No components other than those listed. No placeholder or lorem text. No watermark.
```

Faithfulness note: nothing may connect the six runs to each other. They are six independent trajectories that separate at the first noise level and never meet, and chaining them would draw them as one run measured six times, the most misleading thing this figure could say. The support and null slots must stay empty: at every step size tried the bar returned inconclusive. The single-device note must span the runs and the measurement, because the comparison is only fair when all six sat on one card.

### Process capstone: The whole arc as one line

Navigation: ⬅️ [Previous](#prompt-9-process-how-the-correctors-number-was-made) | 📋 [TOC](#table-of-contents)

[planned] ⏳ not rendered
Save as: `diagrams/report-p09-the-whole-arc.png`

```
Style: none prescribed. Do not describe a visual style, color palette, icon set, or rendering technique. Let the composition and visual treatment that best communicate this subject emerge from the context below, rather than from an imposed convention.

Scene: landscape, 16:9. One continuous journey read left to right across the full width, with eight numbered stops. The journey's left-to-right order is the order a reader should meet the results, and explicitly not the order the experiments happened. The route begins in the condition of the uncorrected method and changes, once and only once, between stops 3 and 4, into the condition of the corrected method, staying that way through stops 5 and 6. Between stops 6 and 7 a wide detour leaves the route, passes through ten small unnamed stops, and rejoins without having changed the route's condition at all. The route continues into stop 8 and ends there unfinished, drawn as an opening rather than a close. Three phase groupings sit behind the route: the first covering stops 1 and 2, the second covering 3 to 6, the third covering 7 and 8. Above each stop sits the outcome of that rung: stop 1 two separate animals passing, stop 2 one fused animal failing, stop 3 two separate animals passing, stop 4 a measurement rather than a picture, stop 5 passing, stop 6 mixed passing and failing, stop 7 one fused animal failing, stop 8 empty and unfinished.

Cast: the reading route; eight stops, which are when it works, where it breaks, the correction causes it, what it is made of, the adapter, unseen pairs, ten cheaper fixes, and still open; ten unnamed stops on the detour standing for the ten alternatives; three phase groupings; eight outcomes; an unfinished ending.

Flows: one route changing condition exactly once, between stops 3 and 4. One detour leaving and rejoining between stops 6 and 7, unchanged by the trip.

Text in the image: title "read the results in this order". Phase titles: "the failure", "the fix", "the alternatives and what is left". Stop labels: "1 when it works", "2 where it breaks", "3 the correction causes it", "4 what it is made of", "5 the adapter", "6 unseen pairs", "7 ten cheaper fixes", "8 still open". Detour label: "none of these changed the line".

Exclusions: no dates or timestamps anywhere, since left to right is reading order and not time. No suggestion that the experiments happened in this order. The detour must not change the route's condition. The ending must not be closed off. No placeholder or lorem text. No watermark.
```

Faithfulness note: the detour must rejoin the route in the same condition it left, because none of the ten alternatives changed the outcome, and stop 8 must end open rather than closed, because the arc ends in unfinished work. The condition change must fall between stops 3 and 4 and nowhere else, since rung 3 is where the correction first enters the story. No date may appear: the left-to-right axis is reading order, not a chronology.
