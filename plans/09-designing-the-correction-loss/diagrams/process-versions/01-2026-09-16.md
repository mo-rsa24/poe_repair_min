# Process lane, version 01: 2026-09-16

The initial lane, authored by `populate-plans` from the four plan files this scope now holds.

Three stages and a route map, drawn in the map's pinned art direction (Empty, level 2) with the
subject lane's cast under operation. The stages are the plan set in running order: fix the
instruments (step 71), build the foundation and read what already ran (steps 72 and 73, drawn as
one picture because neither trains anything), and change one thing (step 74).

Every gate's consequence comes from its plan's own `## What has to pass before this runs` section.
None loops back to its own stage, because none of these failures is repaired by repeating the work.

The lane below is the copy as written. It is regenerated whole, never patched, and the next
`sync-plan-tree` pass that finds the plan set changed writes version 02 beside this one.

---

Prompts, in file order:

1. Fix the instruments before measuring anything → `corrloss-p01-fix-the-instruments-first.png`
2. Read what is already on the disk → `corrloss-p02-read-what-is-already-there.png`
3. Change one thing, and prove the two halves agree first → `corrloss-p03-prove-they-agree-first.png`
4. Capstone: one route from an unmeasured objective to one that is named → `corrloss-process-capstone-unmeasured-to-named.png`

None rendered at the time of writing.
