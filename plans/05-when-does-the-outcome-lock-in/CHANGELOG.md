## 2026-09-02
Widened plan 01 ("basins by hand") from one unspecified pair-and-seed run to two:
`a_cat__x__a_dog` (the failure pair the rest of the scope is built around) and
`a_butterfly__x__a_flower_meadow` (an existing cooperative-pair reference, outside the
constrained animal training pool, already composing without correction). The original single
run couldn't tell a stable correct ending from a stable wrong one, since the perturb-and-finish
test measures commitment, not correctness. Running both pairs checks whether basin structure is
a general property of the composed sampler or an artefact specific to composition failure,
while keeping the failure pair as the only gate on whether the scope continues.
