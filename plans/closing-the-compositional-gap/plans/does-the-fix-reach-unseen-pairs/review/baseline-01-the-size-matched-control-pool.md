# 🔬 Review: would any same-size pool have won?

Verdicts for [../plans/baseline-01-the-size-matched-control-pool.md](../plans/baseline-01-the-size-matched-control-pool.md). Questions written
before the run.

## Run kind
**Produces a competitor** (the size-matched control the transfer claim is judged against). It
freezes once it lands: no tuning it up after the animals number is known.

## Runs

| Run | Kind | Launched at | Output | State |
|---|---|---|---|---|
| (none yet; one mixed-pool run, same N, same held-out set) | Produces a competitor | | contrast per held-out pair | not started |

## The pre-registered bar

- [ ] ⚠️ Does the animals pool beat a size-matched mixed pool on the identical held-out animal
      pairs? Same N kills the more-data explanation, so a win attributes transfer to the pool's
      content. A tie or a loss bounds the claim and goes in the paper as that boundary.
