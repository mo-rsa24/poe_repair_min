# 🔬 Review: the three-space corroboration

Verdicts for [../plans/hypothesis-05-the-same-story-from-three-sides.md](../plans/hypothesis-05-the-same-story-from-three-sides.md). Questions written
before the probes run.

## Run kind
**Tests the claim** (Goal 1's independent reads). None of these may move the dose result; they
corroborate it or they surface a diagnosis.

## Runs

| Run | Kind | Launched at | Output | State |
|---|---|---|---|---|
| (none yet; L1, L3 and the quality check can run now from cache, the other two wait on plan 03's images) | | | | not started |

## Written before the run, answered after

- [ ] ⚠️ Does the text-embedding additivity gap predict the correction's size per pair? Report
      the correlation either way: a null means binding lives in joint processing, not in the
      embedding, and that is a finding.
- [ ] ⚠️ Is there a shared binding direction across pairs (cosine matrix plus SVD of
      e_J − normalised(e_A+e_B))?
- [ ] ⚠️ Are chimeras wrong CONTENT rather than bad images? Quality proxies on cached PoE vs
      Mono outputs, expected gap near zero. This is the check that removes "the correction just
      improves image quality" as an objection.
- [ ] ⚠️ Do the dose-sweep outputs slide out of the blend region of the image manifold as λ
      rises, while a random-direction path does not? Feeds slot F5.
- [ ] ⚠️ Does a caption bank read the images back as the blend caption at low λ and the
      two-animal caption at high λ, with a crossover?
