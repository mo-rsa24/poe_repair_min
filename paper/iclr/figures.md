# The figure register: every figure the paper will carry

One row per figure, including figures whose experiment has not run yet. A figure not in this
register is incidental by definition, however good it looks: this file is what separates the
handful of load-bearing images from the 1600 figure files in the repo.

**A reserved slot is a figure planned before its result exists.** It states the claim the figure
will make, the layout that would show it, and the review question that must be answered before
the caption may make that claim. `plan_pulse` watches the slots: when a slot's review question is
answered, it reports the slot as fillable at session start; if the question is answered against
the slot's claim, it reports the slot as broken, which is a contradiction arriving through the
figure door and routes to the diagnosis procedure in `~/.claude/RESEARCH_PRACTICE.md`.

Status: `reserved` (waiting on its run), `fillable` (question answered, figure not built),
`built` (file exists and is referenced from the tex), `broken` (the result does not fit the
claim; do not build, diagnose).

| # | Status | The claim the caption will make | Layout | Answered by | File |
|---|---|---|---|---|---|
| F1 | **built** | PoE composition fails in a specific way: one blended animal, not two | two panels, no chart. Left: two concept densities and their product, drawn by `scripts/make_f1.py` and labelled `schematic, not measured` inside the panel. Right: the real cat × dog seed 9 image at λ=0, same height, joined to the product's peak by the figure's only arrow. The caption names the cell and claims nothing about frequency | no run: the cell exists at `outputs/interaction_term/dose/pairs/a_cat__x__a_dog/seed_9/teacher_residual_const_lam000/` and was checked by eye, one head with cat ear, eye and whiskers on the left and dog ear, muzzle and tongue on the right. The λ=0 rate is owed to the results section, not to this figure. Built by [figure-01](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/plans/figure-01-the-seven-paper-figures.md) | `figures/what-the-product-misses-explainer.pdf`, `python scripts/make_f1.py` |
| F1b | **built** | the same composition rule succeeds on one prompt pair and fails on another, and a correction it never applies is present in both trajectories | two columns, two rows. Columns are the prompt pairs, a butterfly × a flower meadow and a cat × a dog, named above their own column. Top row is one real uncorrected sample each, λ=0, seed 42, seed chipped inside the panel. Bottom row is ‖r_t‖ divided by ‖ε_PoE‖ against denoising step 0 to 49, the size of the correction as a fraction of the prediction it would correct, median over seeds 4, 42 and 123 as the line with a min-to-max band, y axis labelled `residual` and x axis `denoising steps`, on a white page. **Both bottom panels share one y axis**, since the reader is meant to compare them. The exact quantity the axis shows lives in the caption, not on the axis. Median over steps chipped on each panel. fp16 upcast to float32 before every norm | **no review question owns this slot.** Read from the cached residual tensors: `delta` is r_t and `eps_poe` sits beside it in the same file, 50 steps per cell, under `/datasets/mmolefe/poe_repair_min/outputs/veracity/pairs/<pair>/seed_<n>/teacher_residual_const_lam000/residuals/`. Butterfly × meadow median 0.065 over steps, cat × dog 0.224. **Three caption caps.** It says nothing about R_t, the ratio of true co-occurrence probabilities, which is measured nowhere; r_t is a difference between two of one network's noise predictions and the step between them is the one §5.5 refuses to take. Two pairs cannot support any claim that correction size predicts which pairs fail, and on these two the failing pair's correction is the larger, so a size claim would point against the paper. The two curves also differ in shape, early spike against late rise, one cell each, and nothing in the text interprets that | `figures/correction-size-over-the-denoising-run.pdf` with sidecar `.json`, and `figures/samples-as-the-seed-changes.pdf` (all three seeds, images only, no curves), both by `python scripts/make_f1b.py` |
| F2 | **built** | the correction is what is missing: adding the right signal restores two-object scenes, while three matched fakes do not | 4x5 grid of real cells (rows: own r_t, other pair's r_t, other seed's r_t, own r_t with the step order shuffled; column per strength) above the four curves, shared λ axis with column centres on the ticks. All injected vectors are rescaled at each step so their size is comparable. Control rows drawn identically to the oracle row, never greyed: demoting them would say which row is meant to lose before the reader has looked. Per-panel chip gives the detector's animal count. λ=0 column boxed, because nothing is injected there and all rows are one image. The random-vector control is not a row; it belongs in the appendix and answers only "does any disturbance help" | [review](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/review/hypothesis-02-more-correction-more-composition.md), bar ✅. The real correction moves images toward two-object scenes, while the other-pair, other-seed, and shuffled-step controls stay close to the original PoE failure mode. **The caption should state this qualitative contrast rather than quote summary numbers.** The other-seed row's reading was declared before its run: staying flat means the correction is state-specific, so it must be computed per run rather than stored, which is what the adapter is; D2 is the geometric picture of the same fact. The steps-shuffled row staying flat says the content must arrive on time, corroborating F4 from the injection side | `figures/compose-rate-as-correction-rises.pdf`, assembled by `scripts/make_f2.py`; the two control rows generated by `scripts/mechanism_study/run_dose_sweep_controls2.sh`. Grid cell: cat × dog seed 10 (composes from λ=0.5, controls all single-animal; note the running-example doctrine names seed 9, and the grid cell disagrees with it, a standing mismatch to resolve at caption time). `figures/compose-rate-as-correction-rises-with-a-random-control.pdf` for talks. `figures/compose-rate-as-correction-rises-for-a-dissimilar-pair.pdf` is elephant × penguin, held for the "they only fuse because they look alike" objection. |
| F2b | **built** (held in reserve, not in the main flow) | the pair does not fuse merely because the two animals look alike | F2's layout exactly, on elephant × penguin instead of cat × dog, so the two can be laid side by side without the reader learning a second figure. Rebuilt at four rows when F2 was, so the two never disagree about which controls exist | same review and same sweep as F2. Detected counts on the drawn cell (seed 10) read 1,1,1,2,2 down the own-r_t row and 1,1,1,1,1 on all three controls. **This figure is answering an objection, so it is only shown if the objection is raised**; it is not part of the argument chain and its caption may not be read as a second measurement of the effect | `figures/compose-rate-as-correction-rises-for-a-dissimilar-pair.pdf`, `python scripts/make_f2.py --pair an_elephant__x__a_penguin --name compose-rate-as-correction-rises-for-a-dissimilar-pair` |
| F3 | **built** | the correction's size follows noise level, one shared curve across pairs | one panel, one thing to look at: ‖r_t‖ relative to the prediction it corrects, each curve scaled to its own median, against the denoising step 0 to 49 with "noise" and "image" at the ends. The 17 pool pairs drawn as a median line plus a middle-half and a tenth-to-ninetieth band, legend of three short words, y-axis labelled "relative correction size" with the full definition in the caption. No frames and no named pairs: the reader looks at the band, the caption draws the conclusion. Every drawn line, bands included, is a rolling median over five steps, said on the figure; the raw curves are in `cache_analyses/step_collapse.json`. Spread numbers to the appendix table, not the plot | [hypothesis-04 review](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/review/hypothesis-04-what-the-cached-runs-already-show.md), answered 🟡. On the step axis over the 17 pool pairs with seeds averaged: spread 21.4%, verdict `loose`. Averaging seeds moved it only 22.9% to 21.4%, so the verdict word did not change. **The caption is capped three ways.** It claims the collapse for the pool, not for all pairs: the two named pairs sit outside the pool and both leave the band after step 25. It may not say the collapse holds uniformly, because the pairs agree better late than early (30.0% over steps 0 to 24 against 15.6% over steps 25 to 49; 65.1% at step 0 against 13.1% at step 49). And the frames anchor the axis rather than evidencing the y-value | `figures/correction-size-over-the-denoising-run-across-17-pairs.pdf`, `python scripts/make_f3.py`, what was drawn recorded in `figures/correction-size-over-the-denoising-run-across-17-pairs.json` |
| F4a | **built** | the correction only works if it arrives in the first few steps | every cat × dog cell in the window grid: 9 windows across, 4 seeds down, 36 real images and no selection. Columns are the ten steps the correction was applied during, so time reads left to right. No count is printed on the cells, because the detector disagrees with the pictures on seed 12 | [hypothesis-03 review](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/review/hypothesis-03-when-in-the-run-it-matters.md), peak question answered ✅: 0.656 at window 0-10, 0.250 one notch later, 0.000 from 20-30 onward. **The caption may say the effect dies away, and may not say where the best window is**, because the best window measured is also the earliest one the grid can hold | `figures/samples-as-the-correction-window-moves.pdf`, `python scripts/make_f4_grid.py` |
| F4b | **built** | where the correction is large is not where it matters | two panels on one denoising-step axis. (a) how big the correction is at each step, one line per seed, same measure as F3 imported from `snr_collapse.py`. (b) how often it works against the window it was applied during, one curve over all 32 cells per point, not per seed, since one seed at one window is a single run scoring 0 or 1 | same review, both remaining questions answered ✅ and both **no**. Size does not explain timing: 0.48 average over steps 0 to 14 against 1.33 over steps 20 to 40, about 2.7x larger late, every seed peaking between steps 21 and 37, while the composing window is 0 to 10. And the peak does not land at the fork step: centre 15 composes 0.094 against 0.656 at centre 5. **The caption may not say the correction matters because it is large there, and may not draw step 16 as a band behind the timing curve** | `figures/correction-size-per-step-beside-outcome-per-window.pdf`, `python scripts/make_f4_curves.py` |
| F4c | **built** | the timing cliff is visible to an instrument that does not count animals | the same nine sliding windows as F4a, read in language instead: each image is matched against three captions describing two animals and three describing one hybrid creature, and y is the mean CLIP similarity to the two-animal captions minus the same to the hybrid ones, image embeddings L2-normalised. Cat × dog, seeds 9 to 12 | the margin falls from −0.0033 averaged over the early windows to −0.0224 over the late ones, in the same direction and at the same place as the counted cliff, so a second instrument that shares none of the detector's machinery agrees. **The caption may not read the margin as an absolute score**: it is negative everywhere, so CLIP prefers the hybrid captions even for images the detector calls composed, and only the *fall* corroborates the cliff. The sidecar states this limitation in its own field: a flat curve would have been a CLIP limitation, not evidence the images did not change | `figures/language-score-as-the-correction-window-moves.pdf`, `python scripts/make_f4_caption.py`, sidecar `.json` carries the caption banks, the per-seed margins and the detector's verdicts side by side |
| F4d | **built** | timing decides, not dose | 2x2 of real samples, one seed. Rows: the correction applied over steps 0 to 10 and over steps 40 to 50. Columns: the early window's delivered total and the late window's. Each cell chips the total it received. The boxed cell is the late window given exactly the total that composes early | [hypothesis-03 review](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/review/hypothesis-03-when-in-the-run-it-matters.md), the dose confound answered ✅. 16 cells, 4 seeds: every early cell composes at BOTH doses, every late cell fails at BOTH, so the decisive cell is 0 of 4. Tripling the late dose changes the picture almost not at all, and the early window still composes at lambda 2.96, a 3x overdose. **The caption may say timing decides rather than dose.** Verdict is by eye; the detector's counts agree here and are in `dose_matched/swap_scores.json` | `figures/samples-as-window-and-strength-both-change.pdf`, `python scripts/make_f4_swap.py --seed 12` |
| F4e | **built** | the cliff is not a dose effect | the nine-window compose rate twice on one axis: at full strength in every window, and with every window rescaled to deliver the same total. A dotted grey line on a second axis shows the strength each matched window needed | same review. 36 cells, 4 per point, cat x dog only. The matched curve keeps the cliff: 0.75 at steps 0-10, 0.50 at 5-15, 0.25 at 10-20, zero from 15-25 onward. **The windows at 5-15 and 10-20 compose at 0.52 and 0.37 strength**, which refutes the objection that the late tail is just a weak lambda. **The caption may not read this as a population rate**: four cells per point, one pair | `figures/compose-rate-as-the-window-moves-at-matched-total.pdf`, `python scripts/make_f4_matched.py` |
| F4g | reserved | more of the corrected start does not raise the compose rate past the ten-step window's peak | every cat x dog cell in the front-loaded sweep, same layout as F4a: five cutoffs across (0-10, 0-20, 0-30, 0-40, 0-50, correction on for that prefix, plain PoE after), four seeds down, 20 real images, no selection, no count chip. The 0-10 column is F4a's own leftmost cell, not a new run. Backing curve (compose rate pooled over all 8 pairs, same as F4a/F4b) is diagnostic-only, not shipped to the paper: `growing_window_curves.json`, `outputs/interaction_term/window/growing_window_prefix_curve.png` | [hypothesis-03 review](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/review/hypothesis-03-when-in-the-run-it-matters.md), bar not yet written, run in progress | `figures/samples-as-the-correction-runs-longer.pdf`, `python scripts/make_f4g_grid.py` |
| F4h | reserved | once the first steps run uncorrected, no length of correction afterward recovers composition | every cat x dog cell in the back-loaded sweep, same layout as F4a: five cutoffs across (10-50, 20-50, 30-50, 40-50, off, plain PoE for that prefix, correction after), four seeds down, 20 real images, no selection, no count chip. The 40-50 column is F4a's own rightmost cell, not a new run; the last column corrects nothing. Backing curve is diagnostic-only, same location as F4g's | same review, bar not yet written, run in progress | `figures/samples-as-the-correction-starts-later.pdf`, `python scripts/make_f4h_grid.py` |
| F5 | **built** | the λ effect is not an artefact of the detector: three instruments that share none of its machinery move together | three panels. (a) where the picture sits: each cell placed on the axis from its own PoE render to its own Mono render in CLIP image space, against λ, three injected vectors in F2's colours. (b) what it is called: how often the best-matching caption is a two-animal one rather than a single-hybrid one, same axis, with the blend rate dotted behind the oracle so the crossover is visible. (c) which way it pushes: not a dose curve, because the quantity does not depend on λ. One point per cell for how much the correction agrees with the direction the sampler is already travelling, with a wrong-step and a random control beside it | [hypothesis-04 review](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/review/hypothesis-04-what-the-cached-runs-already-show.md), judged per question. (a) 32 cells, the picture slides 0.031 to 0.992 with the dose and only the real correction slides it. (b) 32 cells, two-animal readback 3.1% to 81.2%, crossing the blend rate at λ=0.75, both controls flat. (c) 38 cells, median +0.397 with 0 negative, against +0.114 wrong-step and +0.000 random. **Three caption caps.** The wrong pair still travels 44.0% of the right one's distance against a 50% bar written beforehand, so it passes barely and the caption says so. λ=1 reproduces the joint prediction by construction (endpoint drift 1.9 grey levels of 255), so it carries no evidence and every dose comparison is read at λ=0.75. And panel (c) is measured along the cached PoE path, so it is the push at the states PoE visits, not the climb along a corrected path | `figures/F5-one-dial-three-instruments.pdf`, `python scripts/make_f5.py`, reading `cache_analyses/{manifold_slide_clip,caption_readback,plausibility_climb}.json`, sidecar `.json` |
| F5b | **built** (held beside F5 the way F2b is held beside F2) | the image's meaning is steered from the first steps, and the fork step is the midpoint of that drift rather than the moment the paths part | for each cell both arms are decoded step by step into the model's current estimate of the finished picture, embedded with CLIP, and placed on the axis running from that cell's own PoE endpoint to its own Mono endpoint. y is the two arms' separation along that axis, 0 indistinguishable and 1 the full final separation; x is the schedule timestep, noise left, so a 20-step and a 50-step grid share one axis with no interpolation. 17 pool pairs as a median and two bands, cat × dog and elephant × penguin named on top, the fork band (timesteps 581 to 721) shaded | the pre-registered sharp-window checks failed and the failure is the finding: the steepest rise lands inside the fork band for 3 of 9 cells in CLIP and 1 of 9 in DINOv2, while separation is near zero at step 0 and about half the axis is already covered by the time the band is reached. **Four caption caps.** It does not contradict F4, which asks the causal question the window sweep answers, while this is observational. It says nothing about raw latents, where the fork remains real. The naive read, raw pairwise distance between the arms, is not evidence either way, because two early mush frames are far apart in any embedding for texture reasons. And the 20-step curves rest on estimates recovered in closed form from consecutive latents, validated at 2.6% median error but 14% off at step 0, which is the least trustworthy point on the plot | `figures/how-far-the-corrected-run-separates-from-the-uncorrected-one.pdf`, `python scripts/make_f5b.py`, curves from `scripts/trajectory_divergence.py` and `scripts/dose_trajectory_divergence.py`, sidecar `.json` |
| F6 | **the argument does not stand, slot needs a decision** | the correction is low-rank enough to learn | spectrum with the random floor shaded, held-out projection inset | [hypothesis-04 review](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/review/hypothesis-04-what-the-cached-runs-already-show.md), judged per question rather than by one bar. **The shaded floor gives every row the same expected norm while real ‖r_t‖ spans 4.5x, so it cannot tell shared directions from uneven sizes.** Against random directions carrying the real norms the pooled stack is 1.4x at k=8, not 11x, and the direction structure that survives normalising sits inside single runs (4.8x) rather than across pairs (1.2x), which is D1 and D3 restated. Learnability rests on the adapter's behaviour (96.9% on six unseen pairs against 0% for plain PoE), not on this geometry. Full argument: [F6-what-the-spectrum-measures](../../docs/evidence/F6-what-the-spectrum-measures/QUERY.md) | |
| F7 | **design committed**: its mechanism panel is F7a below | the adapter changes what a word paints more than where it looks, on pairs it never trained on | three bands on one page, sharing F7a's pair order (ascending ratio, cat × dog and elephant × penguin in red). Band one is F7a unchanged. Band two is a table, one row per pair: pair name, mechanism ratio (repeated from band one), compose rate, n seeds, checkpoint step, a rule separating the reference and control pairs from the six transfer pairs. Band three is eight thumbnails in the same pair order, one seed each (seed 9), corrected generation only, same red border convention | [../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/review/hypothesis-01-what-the-fix-changes-inside-the-model.md](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/review/hypothesis-01-what-the-fix-changes-inside-the-model.md), bar answered ✅ (median 1.52 over 64 cells). **Caption capped by the caveat: the effect also shows on the control pair, so the sentence is what the adapter does to any pair it touches, not why the fix works.** Band two needs the phase-1 pooled checkpoint rescored at step 100000 for these eight pairs; `hypothesis-01-does-one-pooled-fix-transfer-at-all` currently has this compose rate only at step 60000, one checkpoint earlier than F7a's own mechanism probe. Not blocked on F8's fifteen-run leave-one-pair-out sweep. | |
| F7a | **built** | the adapter changes what a word paints more than where it looks | horizontal dot plot, one diamond per pair at the median of its 8 seeds, the seeds themselves behind it in pale blue, sorted by median. Dashed line at 1.0 is 'moved both equally'. Running example and dissimilar pair in red. No summary statistic on the plot | [hypothesis-01 review](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/review/hypothesis-01-what-the-fix-changes-inside-the-model.md), bar ✅ and the entity question now answered ✅. All 8 pairs above 1, medians 1.16 (leopard × jaguar) to 2.18 (frog × toad), and the lowest single seed of all 64 cells is 1.07. Median over pairs 1.499, against the pre-registered row-level bar of 1.518 with 97.1% of 384 rows above one. **The caption reports n = 8 pairs and may not quote 384 or 64 as a sample size**, because rows inside a cell are one image read at 3 steps and 2 tokens. **Caption capped by the caveat: the effect shows on the control pair too**, so the sentence is what the adapter does to any pair it touches, not why the fix works | `figures/content-change-relative-to-attention-change-under-lora.pdf`, `python scripts/make_f7.py`, numbers in the sidecar `.json` |
| F8a | **built** | one pooled rank-8 adapter composes on pairs it never trained on, at the rate it manages on pairs it did | two panels on a training-step axis. (a) compose rate for the 11 training pairs and for the 8 pairs held out, with the uncorrected PoE rate as a dotted floor. (b) the held-out result unpooled, one curve per pair, so the aggregate cannot be carried by one easy pair. Labels sit in the panel's empty lower half rather than beside the crowded curves | scored run `outputs/animals_compose_transfer/pooled_lora/phase1_r8_100k`, read from its own `compose_rate.json`. At step 60000: trained-on 0.960 (n=176), never-seen 0.961 (n=128), against a PoE floor of 0.03. Weakest held-out pair is cat × dog at 0.875. **Four caption caps.** Training ran to step 100000 and only checkpoints to 60000 were scored, so every number carries its step and 60000 is the best read rather than the final word. Elephant × penguin composes without any adapter, so its 1.0 is not evidence of transfer and the figure labels it on the panel. The PoE floor is the dose sweep's λ=0 rate whose single positive is a known detector mistake. And "held out" here means pairs never trained on, not a leave-one-pair-out sweep, which is a separate experiment | `figures/compose-rate-as-the-lora-trains.pdf`, `python scripts/make_f8.py`, sidecar `.json` |
| F8b | **built** | the shippable adapter matches or beats the oracle correction it imitates, without needing the joint prompt | one horizontal dot row per pair, four markers: PoE with nothing injected, the oracle correction at λ=0.75, the oracle at λ=1 drawn hollow because it reproduces the joint render by construction, and the adapter at training step 60000. Rows ordered by how much the adapter beats the oracle. Legend above the axes, since every row spans the full width | the two already-scored files, no new runs: `dose_curves.json` for the oracle arm and the pooled run's `compose_rate.json` for the adapter. The adapter is at or above the oracle's λ=0.75 rate on **7 of 8 pairs**; the exception is cat × dog (0.875 against 1.00). **Four caption caps.** Pair-matched, not cell-matched: the oracle arm ran seeds 9-12 (4 cells per pair) and the adapter arm seeds 9-16 (16 cells per pair), so the pair is the sampling unit and a cell-matched version needs the adapter rescored per seed. The λ=1 marker is the joint render itself (endpoint drift 1.9 grey levels of 255), so it is the target and not a method. The adapter is step 60000 with 70000-100000 unscored. And elephant × penguin composes without any intervention, so its row is a control | `figures/compose-rate-by-pair-for-lora-against-the-joint-prompt-correction.pdf`, `python scripts/make_f8b.py`, sidecar `.json` |
| F8 | reserved | transfer degrades gracefully as fewer pairs train (the leave-one-pair-out sweep, distinct from F8a) | leaderboard plus degradation curve | [hypothesis-02 review](../../plans/closing-the-compositional-gap/plans/does-the-fix-reach-unseen-pairs/review/hypothesis-02-transfer-as-a-rate-over-fifteen-pairs.md) plus [baseline-01 review](../../plans/closing-the-compositional-gap/plans/does-the-fix-reach-unseen-pairs/review/baseline-01-the-size-matched-control-pool.md), both bars written, runs not started | |
| D1 | **built** (appendix) | within one run the correction turns smoothly rather than jumping | one plot: agreement (cosine) between consecutive steps' corrections of one uncorrected run against the step, thumbnails of that run forming in the empty axis space, coloured borders, connectors to the axis, thumbnail height carries no data | measured from the training cache, fp32, no sampling. Median +0.967 on eagle × hawk seed 9. **The caption must say the smoothness varies by pair**: same-run adjacent-step medians span −0.32 (cat × dog, which alternates sign mid-run) to +0.97 over the 8 dose pairs × 2 seeds; per-pair values in `outputs/interaction_term/direction_wall/direction_wall.json` | `figures/direction-agreement-between-consecutive-steps.pdf`, `python scripts/rt_direction_companions.py`, what was drawn in the sidecar `.json` |
| D1b | **built** (appendix, beside D1) | one real pair breaks the smoothness pattern, and its correction still composes | the same axes as D1 with no thumbnails: cat × dog seed 9's consecutive-step agreement, which swings between −0.85 and +0.9 through the run. Its frames are D2's top row, named in the panel rather than repeated | measured from the cache: median −0.317 over the run, −0.581 over steps 16 to 32. Read against F2, where this pair's own correction composes at full dose. **The caption states the consequence: temporal smoothness is not necessary for an injected correction to work, so D1's claim is descriptive, not a precondition.** Whether smoothness predicts *adapter* success is untested until F8 | `figures/direction-agreement-between-consecutive-steps-when-it-alternates.pdf`, same script, sidecar `.json` |
| D1c | **built** (appendix, beside D1) | what "no signal" would look like: the falsification floor | the same axes and the same computation run on random norm-matched vectors instead of real corrections. A flat line on zero | median cosine +0.0006, generator seed 0, shape [50, 65536] recorded in the sidecar. **This is the figure that would kill D1 wholesale**: if real corrections looked like this, there would be no signal to learn. They do not: 14 of the 16 measured runs sit at +0.8 to +0.97 | `figures/direction-agreement-for-random-vectors.pdf`, same script, sidecar `.json` |
| D2 | **built** (mechanism companion, placed with F6/F7 or appendix at layout time) | a correction cannot be stored and reused, not even for its own pair: it must be computed from the current image, which is what the adapter is | one plot: agreement (cosine) between the corrections of two runs of the SAME pair (cat × dog, seeds 9 and 13) at matched steps, flat on zero the whole run; above the line the first run's pictures forming (blue borders), below it the second's (green), both visibly fusing toward DIFFERENT single animals | measured from the training cache, fp32: median +0.002, range −0.05 to +0.37 (the +0.37 lives in steps 0-2 only). Behavioural twin: F2's other-seed row at the compose-rate floor, predicted from this geometry before scoring. **The caption may not say the runs succeed: the cached trajectories are the uncorrected PoE paths (decoded endpoints match each cell's poe.png), so the frames show the failure happening two different ways** | `figures/direction-agreement-between-two-seeds.pdf`, `python scripts/rt_reuse_figure.py`, sidecar `.json` records seeds, steps and how frames were decoded |
| D2b | **built** (appendix, beside D2) | the correction is a smooth function of the starting state early in the run and chaotic late, and the early part is the part that matters | one plot, two curves. x is how far the starting noise was moved from seed 9's toward seed 13's (slerp, so the noise magnitude is unchanged); y is how much the resulting correction still agrees with seed 9's. The first-3-steps curve decays gently (+0.996, +0.951 at a tenth of the way, +0.769 at a fifth) and levels off near +0.35; the steps-10-to-49 curve is gone by a tenth of the way | measured by `scripts/rt_noise_interpolation.py`, 8 interpolated runs walked with `run_teacher_residual` at λ=0. Read with F4 (only the first ten steps compose) this says the load-bearing part of the correction behaves like a learnable function of the state. **Two caption caps.** The leftmost point is +0.996 early and +0.427 late rather than 1.0, because rerunning the identical noise still diverges: measured fp16 trajectory drift 0.24% at step 10, 0.93% at step 20, 17.1% at step 49, so every point is read against that ceiling. And the late curve's collapse mixes the correction changing with the state and the two trajectories being at genuinely different states by then, which this figure cannot separate; the early curve carries the claim | `figures/direction-agreement-as-the-starting-noise-is-moved.pdf`, `python scripts/make_d2b.py` after `scripts/rt_noise_interpolation.py`, sidecar `.json` |
| D3 | **built** (appendix) | different pairs' corrections share no direction at any step | same template as D2 with the second row a different pair: cat × dog seed 9 (blue) over eagle × hawk seed 9 (orange), matched steps, flat zero between them | measured from the training cache, fp32: median +0.007 over the run. **The caption must own the visible exception**: agreement starts near +0.6 and dies by step 5, a shared early push that is close to universal (cross-pair medians over steps 0-2: +0.28 across all 28 combos, versus +0.34 within-pair; `outputs/interaction_term/direction_wall/early_agreement.json`), not a pair-specific signal | `figures/direction-agreement-between-two-pairs.pdf`, `python scripts/rt_direction_companions.py`, sidecar `.json` |
| D3b | **built** (appendix, subsumes D2 and D3 into one controlled comparison) | the early correction depends on the starting noise and on the prompt about equally, and a universal component survives changing both; the late correction depends on everything and shares nothing | four conditions, one factor changed at a time, each read at the first 3 steps and at steps 10 to 49. Cells with the same seed start from identical noise whatever the pair (checked: cosine +1.0000), so all four combinations are already on disk | cache-only, fp32. Early / late: same noise and same prompt +0.996 / +0.427 (one rerun, the ceiling); different noise, same prompt +0.321 / +0.003 (80 run-pairs); same noise, different prompt +0.302 / +0.006 (140); both different +0.215 / +0.001 (560). **Two caption caps.** The ceiling row is a single rerun and falls short of 1.0 because fp16 nondeterminism compounds through the sampler, so no row is read against 1.0. And same-seed cells share a state only at step 0, since differing prompts separate their trajectories, which is why early and late are reported separately and the claim is made on the early column | `figures/direction-agreement-by-what-the-two-runs-share.pdf`, `python scripts/make_d3b.py` |
| D4 | **built** (appendix, paired with D2) | the right correction buys composition on the very runs D1-D3 showed failing, each run's fix produces a different scene, and the commitment to one animal or two happens in the first handful of steps | three rows (cat × dog seed 9, cat × dog seed 13, eagle × hawk seed 9), each row four uncorrected frames (dashed borders, the PoE path's own estimates, the same frames D1-D3 use) beside four corrected frames (solid borders, same starting noise, own r_t at full dose) at steps 2, 6, 10, 20. The steps sit inside F4's composing window rather than spread evenly over the run: an even spread starts at step 10, by which point both sides have already committed and the figure shows only the aftermath. At step 2 neither side has committed; by step 6 the uncorrected run is one animal and the corrected run is two | uncorrected frames are Tweedie estimates from the cache; corrected seed 9 from the cross run's saved clean estimates, the other two solved exactly from consecutive λ=1 dose-run latents (a DDIM step is linear in the clean image and the noise). The λ=1 seed-13 run is not a sweep cell; it was generated for this figure and sits outside the scored seeds. **Two caption caps.** Corrected seed 13 reads as a tan dog and a white puppy, so count ≥ 2 calls it composed but the caption may not call that cell "a cat and a dog". And the commitment-by-step-6 observation is three cells shown for illustration; F4's nine-window sweep over 32 cells is what measures when the correction is needed, so the caption points there for the claim | `figures/samples-per-step-with-the-correction-on-and-off.pdf`, `python scripts/rt_correction_payoff.py`, sidecar `.json` records every frame's provenance |

Owed by [../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/plans/figure-01-the-seven-paper-figures.md](../../plans/closing-the-compositional-gap/plans/does-the-correction-cause-composition/plans/figure-01-the-seven-paper-figures.md)
(F1 to F7) and [../../plans/closing-the-compositional-gap/plans/does-the-fix-reach-unseen-pairs/plans/figure-01-the-transfer-figures.md](../../plans/closing-the-compositional-gap/plans/does-the-fix-reach-unseen-pairs/plans/figure-01-the-transfer-figures.md)
(F8). The manuscript currently includes no figures; the `\includegraphics` lines in the tex are
the ICLR template's own examples.

**What a caption may never claim:** anything its review question does not say. A bounded failure
is written as the boundary it found ("holds for X, not for Y"), not softened and not omitted.
